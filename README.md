# S18 UI Starter

UI only. No application logic is implemented.

Files:
- index.html
- styles.css
- app.js

The map is currently a visual placeholder. We will initialize Google Maps in `#map` ourselves in the next step.

Suggested next JS task:
1. Select `#sidebar`
2. Select `#menuButton`
3. Toggle a `collapsed` class
4. Select `#closeSidebar`
5. Toggle the same class
